---
- name: check squid status
  command: jexec squid service squid onestatus
  register: result
  ignore_errors: True

- name: start squid
  command: jexec squid service squid start
  when: "result.rc != 0"

- name: reload squid
  command: jexec squid service squid reload
  when: "result.rc == 0"
